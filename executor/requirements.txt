# Runtime executor — bản tối thiểu. Bỏ comment khi chạy AWS/EKS thật (W12).
requests>=2.31
# kubernetes>=29.0        # urgent path: K8s API (restart/patch/rollout-undo)
# boto3>=1.34             # idempotency (DynamoDB) + audit (S3 Object Lock)
